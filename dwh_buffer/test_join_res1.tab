create table DWH_BUFFER.TEST_JOIN_RES1
(
  val_id       NUMBER(10) not null,
  dep_id       NUMBER(10) not null,
  id           NUMBER(10) not null,
  flzo         NUMBER(1) not null,
  planfl       NUMBER(1) not null,
  fromdate     DATE not null,
  bal_in       NUMBER(18,2) not null,
  bal_out      NUMBER(18,2) not null,
  specfl       NUMBER(1) not null,
  natval_in    NUMBER(18,2),
  natval_out   NUMBER(18,2),
  acc_id       NUMBER(10),
  in_source_id VARCHAR2(81),
  id_bal       NUMBER(10),
  cli_id       NUMBER(10),
  cli_dep_id   NUMBER(10),
  cli_code     VARCHAR2(30),
  acc_code     VARCHAR2(34),
  bal_code     VARCHAR2(30),
  bal3         VARCHAR2(30),
  acc_name     VARCHAR2(250 CHAR),
  a_val_id     NUMBER(10),
  val_code     VARCHAR2(30),
  acc_state    VARCHAR2(30 CHAR),
  state_id     NUMBER(1),
  last_trn     DATE,
  date_open    DATE,
  date_close   DATE
)
tablespace DWH_BUFFER
  pctfree 10
  initrans 1
  maxtrans 255
  storage
  (
    initial 64K
    next 1M
    minextents 1
    maxextents unlimited
  );
grant select on DWH_BUFFER.TEST_JOIN_RES1 to AIBEK_BE;
grant select, insert, update, delete on DWH_BUFFER.TEST_JOIN_RES1 to DWH_BUFFER_FULL_ROLE;
grant select on DWH_BUFFER.TEST_JOIN_RES1 to DWH_BUFFER_READ_ROLE;
grant select on DWH_BUFFER.TEST_JOIN_RES1 to DWH_PRIM;
grant select on DWH_BUFFER.TEST_JOIN_RES1 to DWH_RISK;
grant select on DWH_BUFFER.TEST_JOIN_RES1 to DWH_SALES;
grant select on DWH_BUFFER.TEST_JOIN_RES1 to HEAD_DENIS_PL;
grant select on DWH_BUFFER.TEST_JOIN_RES1 to HEAD_EVGENIY_PI;
grant select on DWH_BUFFER.TEST_JOIN_RES1 to KRISTINA_KO;
grant select on DWH_BUFFER.TEST_JOIN_RES1 to PROCESS_RISK;
grant select on DWH_BUFFER.TEST_JOIN_RES1 to PROCESS_SALES;

